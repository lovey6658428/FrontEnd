export default {
    UI_open(state) {
        return state.UI.open;
    },
    APP_shopType(state) {
        return state.APP.shopType;
    },
    APP_shop(state) {
        return state.APP.shop;
    },
    APP_goods(state) {
      return state.APP.goods;
    }
};
